name: Setup system deps
description: >-
   Ensure the stock GitHub runner has the canvas/native runtime dependencies CI
   needs (cairo/pango stack, fd, ripgrep, and ImageMagick).

runs:
   using: composite
   steps:
      - name: Install missing system deps
        shell: bash
        run: |
           # Avoid apt work when a GitHub image revision already includes the
           # complete dependency set.
           if command -v fd >/dev/null 2>&1 \
              && command -v rg >/dev/null 2>&1 \
              && command -v magick >/dev/null 2>&1 \
              && pkg-config --exists cairo pango 2>/dev/null; then
             echo "System deps already present; skipping apt."
             exit 0
           fi
           sudo apt-get update
           sudo apt-get install -y libcairo2-dev libpango1.0-dev libjpeg-dev libgif-dev librsvg2-dev fd-find ripgrep imagemagick
           sudo ln -sf "$(command -v fdfind)" /usr/local/bin/fd
           sudo ln -sf /usr/bin/convert /usr/local/bin/magick
